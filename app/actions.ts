'use server';

/**
 * Server actions for the blog application.
 * All AI and tag-related actions have been removed.
 */

export async function exampleAction() {
    // Placeholder for future server actions
    return { success: true };
}
